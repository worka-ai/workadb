[package]
name = "workadb-sys"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"
rust-version = "1.89.0"
authors = ["Worka"]
description = "FFI bindings to the embedded workadb (PostgreSQL) engine."

[build-dependencies]
anyhow = "1"

[dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[features]
default = []
